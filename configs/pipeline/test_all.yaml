name: "test_all_models"
device: "cpu"
confidence_threshold: 0.6
iou_threshold: 0.4

# List of models to execute in order
models:
  - name: "vehicle"
    checkpoint: "runs/yolo11_vehicle_seg_v1/weights/best.pt"
  - name: "angle"
    checkpoint: "runs/angle_resnet50_v3/checkpoints/best.pth"  # Update with actual path
  - name: "parts"
    checkpoint: "runs/parts_maskrcnn_resnet50_v1_2/checkpoints/best.pth" # Update with actual path
  - name: "damage"
    checkpoint: "runs/damage_maskrcnn_resnet50_v1/checkpoints/best.pth" # Update with actual path

# Post-processing modules to run after all models
post_processors:
  - name: "damage_to_parts_orchestrator"
