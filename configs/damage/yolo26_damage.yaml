_base_: ../base.yaml

experiment_name: yolo26_damage_seg_v1

model:
  name: yolo26_seg
  stage: damage
  weights: yolo26s-seg.pt

data:
  yaml_path: data/kaggle/data.yaml
  image_size: 640

training:
  epochs: 50
  batch_size: 16
  optimizer:
    lr: 0.01

output:
  runs_dir: runs/damage_yolo26
